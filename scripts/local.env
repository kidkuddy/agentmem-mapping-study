# Points the phd CLI at the database shipped in this repository.
# verify.sh cd's into scripts/ before sourcing this, so $PWD is scripts/.
export PHD_ENV="$PWD/../.no-remote.env"
export EPT_DATA="$PWD/../data"
