# Publishers' full text is never redistributed. The study fetched PDFs and
# extracted page text locally to code from; the decisions made from that text are
# published, the text itself is not.
pdf/
txt/
data/batches/classify-*.json
*.pdf.err
__pycache__/
*.pyc
